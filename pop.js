const numbers = [12, 22, 42, 65, 25];
console.log(numbers);

//delete last element from array

const out1 = numbers.pop();
console.log(numbers);
const out2 = numbers.pop();
console.log(numbers);
console.log(out1, out2);


// PUSH and POP always work on the last element of array